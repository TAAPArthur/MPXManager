MPXMANAGER_BIN_NAME ?= mpxmanager

all: mpxmanager

mpxmanager: $(patsubst %.cpp,%.o,$(wildcard *.cpp)) /usr/lib/libmpxmanager.a
	${CC} $^ -o $(MPXMANAGER_BIN_NAME) ${CPPFLAGS} ${LDFLAGS} -lmpxmanager
